// Compile-time constants stamped by webpack.DefinePlugin (see webpack.config.mjs,
// the tester config). Declared here so tsc typechecks index.ts.

/** Kit version, read from package.json at build time. */
declare const __KIT_VERSION__: string;

/** The deployed webresource name for this artifact, e.g. "new_adaptertester.html". */
declare const __ARTIFACT_NAME__: string;
